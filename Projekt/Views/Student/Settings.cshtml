@model Projekt.ViewModels.StudentSettingsViewModel

@{
    ViewBag.Title = "Settings";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Ustawienia</h2>


<div>

    @Html.ActionLink("Dodaj dane Autoryzacyjne", "Create","Auth", new { id = Model.Student.Id},new{@class=""}) |
    @Html.ActionLink("Edytuj dane autoryzacyjne", "Edit","Auth", new { id = Model.Student.Id},new{@class=""}) |
    @Html.ActionLink("Wróć do listy studentów", "Index","Student")

</div>
